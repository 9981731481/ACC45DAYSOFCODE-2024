#it is my serve
def find_serve(p,q):
    points=p+q
    points=points//2
    if points%2==0:
        print('Alice')
    else:
        print('Bob')
t=int(input('Enter the test case:'))
for i in range(1,t+1):
    p,q=input('Enter the score p and q:').split()
    p,q=int(p),int(q)
    find_serve(p,q)
