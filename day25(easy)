def taste():
    a,b,c,d=input('Enter the ingredients of A B C D').split()
    a,b,c,d=int(a),int(b),int(c),int(d)

    max_tastiness=max(a,b)+max(c,d)
    print('maximum tastiness:',max_tastiness)
t=int(input('Enter the test case:'))
for i in range(1,t+1):
    print('Test case:',i)
    taste()
